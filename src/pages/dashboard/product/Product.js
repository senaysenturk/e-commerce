import React, { useState } from "react";
import CardDetail from "../../../components/shared/card/CardDetail";


const Product = () => {
//   const [cardCount, setCardCound] = useState(0)

//   const addCard = (value) =>{
// setCardCound((prevCount) => prevCount +1)
//   }
  
  return (
    <>

      <CardDetail />

      {/* <div className="add-card">
        <a
        className="add-card-item"
        onClick={(e) =>
        addCard({


        })}/>
      </div> */}
    </>
  );
};

export default Product;
